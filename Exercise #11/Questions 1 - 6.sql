#1 - select count(*) from employee_data where yos >= 4;

#2 - select age from employee_data group by age;

#3 - select age from employee_data group by age order by age desc;

#4 - select age from employee_data where title like "%Data%";

#5 - select age from employee_data order by title desc;

#6 - select perks/salary * 100 from employee_data order by perks desc;